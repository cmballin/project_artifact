<UserControl x:Class="Project_Artifact.VisualKey"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Project_Artifact"
             mc:Ignorable="d" 
             d:DesignHeight="224.561" d:DesignWidth="328.07">
    <Viewbox>
        <Grid>
            <Rectangle x:Name="keyBackground" Height="20" Width="50" StrokeThickness="0">
                <Rectangle.Effect>
                    <DropShadowEffect Direction="270" ShadowDepth="1" BlurRadius="2"/>
                </Rectangle.Effect>
            </Rectangle>
            <!--Border-->
            <Rectangle x:Name="keyBorder" Stroke="#CCFFFFFF" Height="20" Width="50" StrokeThickness="1"/>
            <Label x:Name="keyText" Content="Enter" Grid.Column="1" Grid.Row="1" Foreground="#CCFFFFFF" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10" Background="{x:Null}"/>
        </Grid>

    </Viewbox>

</UserControl>
